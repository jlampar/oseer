import { Dice } from "../../types";

/**
 * An array of UNICODE dice images where every dice is placed at the exact index which corresponds to its numeric value.
 */
export const DICES: Array<Dice> = ["⊠", "⚀", "⚁", "⚂", "⚃", "⚄", "⚅"];
