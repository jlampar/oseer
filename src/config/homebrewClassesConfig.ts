import { ClassesConfig, Class } from "../types";

/**
 * Config with all the homebrew Classes metadata (name, min requirements, PRM formulas).
 * You can add any Class here and it'll be appended to every config.
 */
export const homebrewClassesConfig: ClassesConfig<Class> = [];
