/**
 * The return value of a Modifier of an Ability. It consists of string with placeholders for interpolation and a list of arguments to be inserted inside them.
 */
export type Modifier = [string, Array<string>];
