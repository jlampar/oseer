/**
 * The name of a mode that can be used to run the app: `roll` is rolling 3d6 down the line with/without re-rolls, `analyze` is just inputting scores and getting the metadata and recommendations.
 */
export type Mode = "roll" | "analyze";
