/**
 * A UNICODE representation of dice. The `⊠` is a blank dice, used e.g. for 'analyze' mode when there is no clarity what dices were rolled.
 */
export type Dice = "⊠" | "⚀" | "⚁" | "⚂" | "⚃" | "⚄" | "⚅";
